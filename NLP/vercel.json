{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install -r requirements.txt && python -m spacy download en_core_web_sm && python -m nltk.downloader punkt averaged_perceptron_tagger wordnet"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app.py" }
  ]
}
